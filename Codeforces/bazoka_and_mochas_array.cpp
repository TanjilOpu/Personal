#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc; cin>>tc;
    while(tc--)
    {
        int as;
        cin>>as;
     vector a;

        for(int i=0;i<as;i++)
        {
            cin>>a;
        }
        int in=0,dec=0;
        for(int i=0;i<as;i++)
        {
            if(a[i])
        }
    }
}
